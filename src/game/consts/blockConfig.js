const blockConfig = {
  boxWidth: 1,
  boxHeight: 0.01,
  boxDepth: 1
}

const coinConfig = {
  radius: 0.25,
  tube: 0.05,
  radialSegments: 16,
  tubularSegments: 100
}

const monsterConfig = {
  raidus: 0.25,
  tube: 0.125,
  tubularSegments: 64,
  radialSegments: 8
}

export {
  blockConfig,
  coinConfig,
  monsterConfig
}