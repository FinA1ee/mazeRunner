import MazeObject from './mazeObject';

class Hero extends MazeObject {
  
  constructor(geometry, x, y) {
    super(this, geometry, x, y);
  }

  move(direction) {
    // can move

    // cant move
  }
}